---
import Layout from "@layouts/Layout.astro";
---

<Layout title="script要素">
  <button id="btn" class="btn btn-success">現在時刻</button>
  <script>
    //   <script is:inline>
    const lead: string = "現在時刻：";
    // const lead = "現在時刻：";
    document.getElementById("btn")?.addEventListener("click", () => {
      console.log(`${lead} ${new Date().toLocaleString()}`);
    });
  </script>
</Layout>
